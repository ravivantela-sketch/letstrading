#!/bin/sh

set -eu

if [ -x ".venv/Scripts/python.exe" ]; then
    PYTHON=".venv/Scripts/python.exe"
elif command -v python >/dev/null 2>&1; then
    PYTHON="python"
else
    echo "Python is required to run the local secret scan."
    exit 1
fi

"$PYTHON" tools/secret_scan.py --mode head